# The Vercel deployment is frontend-only. The Flask/Chroma backend runs in a
# container host because its ML dependencies exceed Vercel's function limit.
backend/
requirements.txt
venv/
chroma_db/
.env
.env.local
